# mixtapes/base/package.nix
#
# Base mixtape — the bare stereOS system with no additional agent tooling.
# Everything else builds on top of this foundation.

{ config, lib, pkgs, ... }:

{
  # No extra packages — this is the foundation all other mixtapes extend.
}
